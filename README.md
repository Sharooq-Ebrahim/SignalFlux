# SignalFlux

> A real-time traffic signal control engine written in Go.

## Overview

SignalFlux simulates traffic intersections and makes signal phase decisions (GREEN / YELLOW / RED) in real time. Updates are streamed to connected clients via **Server-Sent Events (SSE)**.